# Redirects are case insensitive. Variable :param and *wildcard values allowed
# as placeholders, e.g.:
#
#   ('/foo/:bar', 'http://example.com/$bar')
#
# will redirect /foo/qux to http://example.com/qux.
REDIRECTS = (
)
